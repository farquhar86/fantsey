<div class="jumbotron center">
  <h1>This is Fantasy Pay</h1>
  <h3> Start a new league or check on a current one </h3>

<% if user_signed_in? %>
<div class="jumbotron center">

<%= link_to "New League", new_league_path, class: "btn btn-primary btn-lg btn-block" %> 
<p></p>

<div id="box">
<table class="table">
  <thead>
    <tr>
      <th>League name</th>
      <th>Buy In Amount</th>
      <th>Number of players bought in</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @leagues.each do |league| %>
      <tr>
        <td><%= link_to league.league_name, league %></td>
        <td><%= league.buy_in_amount %></td>
        <td><%= '#' %></td>
        <td><%= link_to raw('<span class="glyphicon glyphicon-edit"></span>'), edit_league_path(league) %></td>
        <td><%= link_to raw('<span class="glyphicon glyphicon-trash"></span>'), league, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br
</div>

<% else %>
  <h2>The easiest way to make sure all your friends buyin to your fantasy league. </h2> 
  <h3> Sign up or login to create a new leage.</h3>
  <p>
    <%= link_to "Login", new_user_session_path, class: "btn btn-default btn-lg" %> 
    <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary btn-lg" %>
  </p>
  <% end %>
